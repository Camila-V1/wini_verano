//---------------------------------------------------------------------------
#ifndef ColaPrioridadH
#define ColaPrioridadH
//---------------------------------------------------------------------------
#include "UC_puntero.h" // <--- Importante: Usamos la clase de punteros

const int MAXP = 10;

class ColaPri {
private:
    ptr_cola vc[MAXP]; // Arreglo de colas con punteros
    int vf[MAXP];      // Frecuencia máxima por prioridad
    int colaAct;       // Prioridad actualmente activa
    int cant;          // Cantidad actual servida

public:
    ColaPri();
    bool vacia();      // Verifica si toda la estructura está vacía
    int primero();     // Devuelve el valor del primero de la mayor prioridad
    void poner(int elemento, int prioridad);
    void asignarFrecuenciaPrioridad(int frec, int prioridad);
    bool sacar(int &elemento);
    void imprimir(TColor FormColor, TCanvas* Canvas);
};

#endif
